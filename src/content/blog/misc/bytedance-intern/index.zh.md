---
title: "在字节跳动我学到了什么"
description: ""
---

![实习 Offer 邮件](./offer.jpg)

2月那会，我同时拿到了字节和另一家百亿私募的实习 Offer，尽管另一家公司给的更多（1000/day），我还是选择了字节，毕竟是国内互联网巨头之首的宇宙厂。我加入了 data-arch 团队，团队负责“推荐系统架构与服务治理”。这篇文章整理一下我在字节的 takeaways。

## 什么是“推荐系统架构与服务治理”

我主要负责字节内部广泛使用的高性能 RPC 和服务治理框架 Archon 的开发和维护。

### 推荐系统

所谓“推荐系统”，以抖音/TikTok为例，简单来说就是为全球用户，从一个不断变化、数以亿计的内容池里面，实时地挑选出用户可能感兴趣的视频，并呈现。

推荐系统主要分成两个部分在运作，online 和 offline：
- Online：处理用户的**实时**请求，是一个**漏斗式**的多层排序过程，从海量视频中，召回、筛选、排序，最终呈现给用户，处处需要考虑**效率**和**效果**的 trade-off。
- Offline：离线处理海量的用户行为数据和视频内容数据，为 online 系统做准备，完成**特征（Feature）的生产与加工、模型（Model）的训练与迭代，候选（Candidate）的生成和索引**。需要考虑 scalibility 和 precision。

主要的组件有：
- 混排引擎：从推荐引擎和广告引擎，然后进行混合排序，获得一个带有视频和广告的列表，并非简单的穿插，而是基于一个复杂的竞价逻辑排序。
- 推荐引擎：经历了多层漏斗式的召回和排序
  - L0 从亿到万的召回：主要是模型化召回，通过模型学习用户和视频的 embedding 表示，然后用最近邻搜索算法（如 HNSW），快速找到与当前用户向量匹配的 Top-K 个视频。也支持倒排索引召回，根据视频的标签、地点、创作者等召回。还支持规则化召回，例如好友发布的视频。
  - L1 从万到千的粗排：加入用户的部分行为序列特征，如最近点赞的视频，进行初步打分。
  - L2 从千到百的精排：对千级数据进行最精准，最复杂的排序，选出几十个结果，用深度神经网络，融合数百上千的 feature 进行打分，使用全量特征，包括用户长期行为历史，视频深度内容理解等。
- 广告引擎：支持品牌曝光等多种营销场景，具备广告活动分析，智能优化等多种功能。广告推荐原理与推荐系统相似，也包含 Recall、Pre-rank、Rank、Rerank等步骤。

### 服务治理

服务治理是在微服务架构中确保服务可靠、安全、可观测、可管理的一系列实践和工具。字节的数以亿计的用户规模、多样的业务线、快速迭代的业务需求，决定了字节使用微服务架构是其必然选择。微服务架构将单一应用程序拆分成一组小型的、自治的服务，可独立部署扩展，升级，但引入了新的问题，对系统架构带来了更高的要求。

1. 服务实例动态扩缩容，那么如何知道服务的位置？这就需要**服务注册**，例如 Consul，新的服务启动的时候自动注册到注册中心，消费方需要调用服务的时候就要通过**服务发现**从服务注册中心查询可用的实例。
2. 服务可能发生故障，需要防止调用方将请求传递给一个死掉的实例，这就需要**健康检查**，对实例进行探活，服务注册中心只返回健康的实例。
3. 即使实例本身是健康的，也可能因为突发流量而过载，调用方可能出现响应缓慢或错误率升高，此时**熔断**机制可以检测到下游服务异常，调用方快速失败，防止过载，给服务一定的时间进行恢复。也可以开启**出流量限流**，防止过高的负载把下游打挂。与之相对的，下游实例也可以主动进行**入流量限流**，当入口流量过高，主动丢弃一些流量，以换取自身的健康和其他请求的正常。
4. 微服务架构依赖网络通信，而网络通信存在波动，如果请求没有在预期时间内返回，调用方需要设置合适的**超时**事件，避免无限等待，还可以在超时后进行**重试**，但是过多过频繁的重试又可能导致下游被打挂，这需要在可靠性和安全性之间做权衡。
5. 微服务通常部署多实例以实现高可用和水平扩展，那么一个服务的多个实例都能处理请求时，如何决定将请求发给哪一个？这就需要**负载均衡**。负载均衡算法有很多，需要在均匀分流、会话保持、长尾延迟优化里面做权衡。
6. 当服务需要进行升级时，为了避免新版本引入的故障影响所有用户，我们需要**灰度发布**进行缓慢切流，渐进式升级，同时利用**可观测性**的基础设施观察各项指标，一旦新版本出现问题，可以立即回退到旧版本，验证稳定后再逐步扩大范围。
7. 服务间的通信治理逻辑如果都嵌入到业务代码中，会导致业务与治理逻辑耦合，不同语言栈还要各自实现一遍，如何将这些能力从业务中剥离出来？这就需要**服务网格**（Service Mesh），以 Sidecar 代理的方式拦截所有进出流量，将服务治理能力下沉到基础设施层，业务代码只需关注业务逻辑，治理策略由控制面统一下发。

## 成果与成长

### Data Driven 的做事风格

字节是做推荐起家的，其支柱产品如抖音、今日头条均依赖个性化算法，这种业务性质要求公司对数据极其敏感，这也造就了字节的 Data Driven 的做事风格，相比其他大厂，字节更看重 A/B 测试、量化产出。例如我们团队经常会通过实验来验证某个优化对延迟和内存的影响，从而估计应用该项优化

对我个人来说，我认为这种 Data Driven 的做事风格对我影响很大，我认为，不仅在工作中，在其他方面也应该更加关注数据、分析数据，而不是被互联网或周围人的几句没有支撑的话影响自己的决策。
<!-- TOOD -->

## 如何保持高效

1. 制定明确的 DDL，不是上级给自己定的，而是自己给自己定的。如果一直把一件事情定在稍后再做就会一直拖延，如果定在本周四中午 12:00 之前完成，就会有一种紧迫感。
2. 善用 AI，AI 工具可以极大地提效。字节有很多仅内部使用的 AI 工具，在这方面字节还是非常领先的。
3. 多和同事同步，有的问题自己想半天没想出来，但是和熟悉这部分代码的同事沟通一下就有好办法了。
4. 上下文很重要，context 不仅对 agent 重要，对人类的也很重要，Archon 作为一个 17 万行代码的巨型代码仓库，有很多不理解的地方，其实可以在飞书里搜到相应的文档，在 file history 里面找到相应的 commit 看这么写的原因，如果不理解代码的原始意图，就无法修改。
<!-- 例如之前本来想要怀疑粘包场景会不会有问题，不知道怎么测试，在群里沟通了一下，同事之前修过一个粘包的 bug，给了我现成的测试方法，很快解决了我的问题。 -->
<!-- 
还比如有一次发版之后，线上报告 81100 明显增加，原来是一个同事引入的特性存在 BUG，长链接复用的时候，在 socket 创建的时候，会设置 socket 关闭的 callback 是从它所在的 addr pool 里面删除它。重构之后，会在 connection 内部错误的时候换一个 pool 进行重试，这会把 socket 绑定到另一个 addr pool 里面，但是删除的时候还是从原来的 pool 里面移除，导致后面的 pool 中存在无法被销毁的死 socket，出现偶发的异常。虽然这个问题发生概率小，且影响夜宵，但是业务方出现人传人现象，也就是某个业务发现统计数据里面 81100 增加，于是在群里面问其他业务，导致其他业务也发现 81100 增加，所以就拉了很多人的大群来解决。 -->

## 技术成长

### OptimizedServer IDL Zero Copy 优化

#### 背景

Archon 框架使用 thrift IDL 定义 RPC 的接口，然后使用 archon-gen 生成 client 和 server 的 C++ 代码。

零拷贝优化具体可以指很多东西，例如 mmap 内存映射、MSG_ZEROCOPY、buffer复用等，我这里做的优化主要是后面两种。
<!-- 

我在维护一个 RPC 框架，这个 RPC 框架通过 thrift IDL 定义接口，我们支持了在 IDL 中定义 binary 类型 ，这样，客户端可以把裸二进制的指针放入 struct，然后经过 thrift 序列化之后，存入  write buffer，通过网络发送到我们的服务端，服务端的 read buffer 里面就有了一块区域储存这个二进制 type | len | data，RPC 框架反序列化为 struct，传递给 server handler

这里的拷贝是写读各一次，客户端将二进制的指针放入 struct 之后，序列化的时候需要将巨大的二进制序列完整拷贝一份到 write buffer 里面。

问：为什么不能提前分配，客户直接写入到指定位置，理论上可以，但是这个特性就是允许客户把业务代码里面用到的 binary 直接放进 rsp 的 struct，如果改成提前分配，用户的业务代码可能大改，另外提前分配难以估计序列化之后前后需要多少空间，还有多个 binary 的情况，在实现复杂度、易用性和兼容性上都会差很多。

优化方案：

这里不能指针拷贝，根本原因在于 write buffer 是连续内存，如果要传递指针，必须改成 linked list of buffers，也就是使用 `folly::IOBuf`，代替原本的 (ptr, len)。这样工作量虽然大，但是效果好，而且利于后续进一步优化工作的开展。

原本用的是 thrift 提供的 MemoryBuffer，我写了一个 MemoryIOBuf，内部使用 folly::IOBuf 代替连续 buffer，然后基于这个修改 TConnection 的逻辑。

做的其他优化：

1. 原本的实现中，write buffer 和 read buffer 一样，在连接建立的时候分配，这样会导致连接建立慢，还会影响 oneway 请求的总延迟。我优化成了连接建立的时候只分配 read buffer，然后调用 handler 之前才分配 write buffer。为了实现这个优化，把原来的 MemoryIOBuf 分成了 reader 和 writer 两个实现，这样才能让两边的逻辑不同。
2. 加了一个 read buffer 的初始化大小可配置选项，之前是固定 1k，动态扩容，由于部分业务对延迟敏感而对内存使用不敏感，加了一个flag，对延迟敏感的业务调整到 2M 即可。

测试与验证：

1. 零拷贝验证：为了防止过长的链路中间疏忽导致拷贝了 buffer，我用日志打印了入站 IOBuf 每一个节点，handler request 中的 binary，handler response 中的 binary，出站 buffer IOBuf 的每一个节点，地址范围，比对 binary 的地址被包含在 IOBuf 中，确认中间没有多余拷贝。

2. 端到端验证：由于公司内部的 RPC 框架有多种协议，还有小包中包大包多种情况，还需要兼容 UStack。我首先写了一个脚本，遍历使用的（theader, ttheader, tframed) * (1K, 1M, 10M) * QPS 写了个脚本自动本机测试，观察没有出现错误，然后由于开发机环境不支持 UStack（用户态协议栈），因此需要申请 UStack 测试机进行端到端测试，UStack 测试机不支持本机回环，必须两机对打，ssh到两台机器上面遍历参数很麻烦，于是询问大模型有没有好的方法可以自动遍历所有参数，ssh到两台机器上面，大模型说可以用 expect，于是让大模型写了脚本，我自己再调了一下。这个脚本后面也沉淀到文档里面，用于一些其他特性分支与 UStack 的兼容性测试。

3. 线上引流测试：用线上的服务 index_service 单 pod 替换二进制到新版本进行线上引流测试，测试结果是延迟降低4%，内存降低 2%。

遇到的困难：
1. review：代码进行了多轮 review，有一个接口只在 folly22 存在，folly18上不存在，clearAndTryReuseLargestBuffer，加了一个宏，如果不支持就回退到 clear，mentor担心这个对性能还是有影响，于是手写了一个folly18下的polyfill。
2. revert：代码上线几个小时内，由于被发现一个 borrow 的语意写错了，代码进行了回滚，后面检查发现这个 borrow 没有用到，但是还是被告知更地审核一下代码再上线。

如何阅读大型代码：
1. 从作用入手，理清每个主要类的功能
2. 理清类与类之间的作用
3. 善用动态调试
4. 重点关注执行流或数据流越过边界的位置
  - 执行流断开：如 callback，EventLoop，Listener这种
  - 数据流断开：如 queue，Dispatcher，I/O这种
5. 善用动态调试，大量使用虚基类的代码，用gdb可以快速看出动态分派的函数
-->

### RDMA 接口对齐

某线上业务由于内存不足，主动调用 `setThreadStackSize()` 调小了线程栈空间，后面 Archon 引入 RDMA（远程直接内存访问，一种高性能网络技术），业务启用后，发现爆内存。

排查原因后发现，由于 RDMA 服务器是在 `enable_rdma()` 接口调用的时候立即创建的，因此 `setThreadStackSize()` 调用时，RDMA 服务器已经创建，设置线程栈大小的配置根本应用不到 RDMA 服务器上，且诸多其他接口，在 RDMA 模式下都不可用。

解决方案：

1. 延迟化 RDMA 服务器初始化：在 `enable_rmda()` 调用时只记录一个 enable 配置项，实际的 RDMA 初始化推迟到 `serve()` 命令真正调用时。
2. 增加一个 `RdmaServerOptions` 选项，用来统一 RDMA 配置，然后将 RDMA 模式下可用的配置与 TCP 模式对齐，不可用的配置在文档中说明。