这是一篇 **仅有中文版本** 的测试文章。

当用户访问 `/en/blog/zh-only` 时，应当：

1. 顶部出现 fallback 提示
2. 正文显示这段中文内容
3. `lang` 属性应被设置为 `zh`

## 二级标题

一些段落内容，验证排版效果。

> 这是一段引用，用于检查样式渲染。

```ts
// 一段示例代码
const greet = (name: string) => `你好，${name}`;
```
